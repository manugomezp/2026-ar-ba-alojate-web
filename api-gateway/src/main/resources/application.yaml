spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: publicaciones
              uri: lb://publicaciones
              predicates:
                - Path=/alojate/publicaciones/**, /alojate/multimedia/**
              filters:
                - RewritePath=/alojate/publicaciones(?<segment>.*), /api/publicaciones${segment}
                - RewritePath=/alojate/multimedia(?<segment>.*), /api/multimedia${segment}
            - id: reservas
              uri: lb://reservas
              predicates:
                - Path=/alojate/reservas/**, /images/**
              filters:
                - RewritePath=/alojate/reservas(?<segment>.*), /api/reservas${segment}
            - id: usuarios
              uri: lb://usuarios
              predicates:
                - Path=/alojate/usuarios/**
              filters:
                - RewritePath=/alojate/usuarios(?<segment>.*), /api/usuarios${segment}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-h3xrizjs1bocf38m.us.auth0.com/
          audiences: https://localhost:8080
auth0:
  audience: https://localhost:8080
server:
  port: 8080


#            - id: publicaciones_route
#              uri: http://localhost:8090
#              predicates:
#                - Path=/api/publicaciones/**
#              filters:
#                - RewritePath=/api/publicaciones/(?<segment>.*), /alojate/publicaciones/${segment}
#            - id: reservas_route
#              uri: http://localhost:9000
#              predicates:
#                - Path=/api/reservas/**
#              filters:
#                - RewritePath=/api/reservas/(?<segment>.*), /alojate/reservas/${segment}
#            - id: usuarios_route
#              uri: http://localhost:9010
#              predicates:
#                - Path=/api/usuarios/**
#              filters:
#                - RewritePath=/api/usuarios/(?<segment>.*), /alojate/usuarios/${segment}
